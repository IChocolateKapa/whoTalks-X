//
   Created by Echo on 2015/12/18.

extends ../base

block content
    div.main_wrap
        p.pageTitle.color-primary 账户登录
        div.dialog
            form(action='/index')
                input(type='text', placeholder="请输入用户名", autocapitalize='off', required="true", id="phone").input.input-mid.color-default.w_100

                input(type='password', placeholder="请输入密码", autocapitalize='off', required="true", id="phone").input.input-mid.color-default.w_100

                input(type='submit' class="btn danger w_100 input-mid", value="立即登录")
